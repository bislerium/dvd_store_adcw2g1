@using dvd_store_adcw2g1.Models.ViewModels
@model IEnumerable<dvd_store_adcw2g1.Models.ViewModels.DVDonShelves>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DVDTitle.DVDTitleName)
            </th>
              <th>
                @Html.DisplayNameFor(model => model.Actor.ActorSurname)
            </th>
           
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
             <td>
                @Html.DisplayFor(modelItem => item.DVDTitle.DVDTitleName)
            </td>
             <td>
                @Html.DisplayFor(modelItem => item.Actor.ActorSurname)
            </td>
            
           @* <td>
                <a asp-action="EditPost" asp-route-id="@item.DVDNumber">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DVDNumber">Details</a> |
                <a asp-action="DeleteConfirmed" asp-route-id="@item.DVDNumber">Delete</a>
            </td>*@
        </tr>
}
    </tbody>
</table>